{
  "name": "giro-estoque",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build && node scripts/copy-vendor.mjs",
    "test": "node --test",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "vite": "^8.3.0"
  }
}
